<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			 xmlns:localFormFields="clr-namespace:Mimeo.DynamicUI.Avalonia.FormFields;assembly=Mimeo.DynamicUI.Avalonia"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:Class="Mimeo.DynamicUI.Avalonia.DecimalField"
			 x:DataType="localFormFields:FormFieldViewModel">
	<UserControl.Resources>
		<localFormFields:DecimalPlacesToFormatConverter x:Key="DecimalPlacesToFormatConverter" />
		<localFormFields:DecimalPlacesToIncrementConverter x:Key="DecimalPlacesToIncrementConverter" />
	</UserControl.Resources>
	<NumericUpDown Value="{Binding Value}" 
				   FormatString="{Binding FormFieldDefinition.DecimalPlaces, Converter={StaticResource DecimalPlacesToFormatConverter}}"
				   Increment="{Binding FormFieldDefinition.DecimalPlaces, Converter={StaticResource DecimalPlacesToIncrementConverter}}">
		<NumericUpDown.IsEnabled>
			<MultiBinding Converter="{x:Static BoolConverters.Or}">
				<Binding Path="!FormFieldDefinition.ReadOnly" />
				<Binding Path="!FormFieldDefinition.Disabled" />
			</MultiBinding>
		</NumericUpDown.IsEnabled>
	</NumericUpDown>
</UserControl>
